{
  "name": "buyat",
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:template": "pnpm --filter template start --o --live-reload true --disable-host-check",
    "dev:www": "pnpm --filter www start --port=4201",
    "prepare": "husky install"
  },
  "dependencies": {
    "husky": "^8.0.3"
  },
  "devDependencies": {
    "prettier": "^3.0.3",
    "husky": "^8.0.0"
  }
}
